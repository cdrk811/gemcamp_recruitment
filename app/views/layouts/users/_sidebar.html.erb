<div class="admin-sidebar-menu overflow-auto bg-dark d-flex flex-column px-8 w-200" data-controller="admin--sidebar" data-admin--sidebar-target="sidebarList">
  <ul id="sidebar-list" class="" data-action="click->admin--sidebar#toggle">
    <li class="home">
      <%= link_to user_root_path, class: 'block' do %>
        <span class="btn btn-dark fs-8 text-white-100 w-100 text-start"><%= t('view.common.home_page') %></span>
      <% end %>
    </li>
    <li class="menu my-2">
      <button class="sidebar-toggle btn btn-dark fs-8 text-white-100 w-100 text-start">
        <span><%= t('view.backstage.member_management') %></span>
      </button>
      <ul class="member_management">
        <li>
          <%= link_to applicants_path, class: 'btn btn-dark w-100 text-start' do %>
            <span><%= "#{Applicant.model_name.human} #{t('view.common.management')}" %></span>
          <% end %>
        </li>
        <li>
          <%= link_to batches_path, class: 'btn btn-dark w-100 text-start' do %>
            <span><%= "#{Batch.model_name.human} #{t('view.common.management')}" %></span>
          <% end %>
        </li>
        <li>
          <%= link_to pre_class_schedules_path, class: 'btn btn-dark w-100 text-start' do %>
            <span><%= "#{Phase::PreClassSchedule.model_name.human} #{t('view.common.management')}" %></span>
          <% end %>
        </li>
        <li>
          <%= link_to pre_class_results_path, class: 'btn btn-dark w-100 text-start' do %>
            <span><%= "#{Phase::PreClassResult.model_name.human} #{t('view.common.management')}" %></span>
          <% end %>
        </li>
        <li>
          <%= link_to gem_camps_path, class: 'btn btn-dark w-100 text-start' do %>
            <span><%= "#{Phase::GemCamp.model_name.human} #{t('view.common.management')}" %></span>
          <% end %>
        </li>
      </ul>
    </li>
  </ul>
</div>
